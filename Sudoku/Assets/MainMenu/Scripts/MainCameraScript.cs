using UnityEngine;
using System.Collections;

public class MainCameraScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//Transform goParent = transform.parent;
		//float widthToBeSeen = (float)goParent.transform.GetChild (1).lossyScale.y;
		//var v3Pos = Vector3(0.0, 1.0, 0.25);
		//print(transform.name);
		//transform.position = gui.camera.ViewportToWorldPoint(v3Pos);
		//Camera.main.orthographicSize = (float)widthToBeSeen * (float)Screen.height / (float)Screen.width * (float)0.5;
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
